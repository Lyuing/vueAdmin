export interface ThemeColors {
  primary: string
  success: string
  warning: string
  danger: string
  info: string
}

export interface ThemeConfig {
  name: string
  label: string
  colors: ThemeColors
}
